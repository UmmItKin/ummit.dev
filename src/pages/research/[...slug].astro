---
import PostLayout, { createPostPaths } from '@/components/PostLayout.astro'
import type { CollectionPosts } from '@/types'

export const getStaticPaths = () => createPostPaths('research')

type Props = { post: CollectionPosts }

const { post } = Astro.props
---

<PostLayout post={post} />
